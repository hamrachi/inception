services:
  mariadb:
    image: mariadb:mariadb
    build: ./requirements/mariadb
    container_name: mariadb
    restart: always
    env_file:
      - .env
    volumes:
      - mariadb_volume:/var/lib/mysql
    networks:
      - inception_net_hamrachi
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "--silent"]
      interval: 5s
      timeout: 10s
      retries: 10
      start_period: 20s

  wordpress:
    image: wordpress:wordpress
    build: ./requirements/wordpress
    container_name: wordpress
    restart: always
    env_file:
      - .env
    volumes:
      - wordpress_volume:/var/www/html
    networks:
      - inception_net_hamrachi
    healthcheck:
      test: ["CMD", "nc", "-z", "localhost", "9000"]
      interval: 10s
      timeout: 10s
      retries: 10
      start_period: 20s
    depends_on:
      mariadb:
        condition: service_healthy

  nginx:
    image: nginx:nginx
    build: ./requirements/nginx
    container_name: nginx
    restart: always
    ports:
      - "443:443"
    volumes:
      - wordpress_volume:/var/www/html
    networks:
      - inception_net_hamrachi
    depends_on:
      wordpress:
        condition: service_healthy

volumes:
  mariadb_volume:
    driver: local
    driver_opts:
      device: /home/hamrachi/data/mariadb
      type: none
      o: bind

  wordpress_volume:
    driver: local
    driver_opts:
      device: /home/hamrachi/data/wordpress
      type: none
      o: bind

networks:
  inception_net_hamrachi:
    driver: bridge
